# Intake

Untriaged observations land here before promotion into `knowledge/`.

Use one file per observation or tightly related observation set. Preserve provenance first; polish later.

Recommended statuses:

- `raw` when captured
- `candidate` after first interpretation
- move or rewrite into `knowledge/` only when reusable beyond the original conversation or task

Do not treat intake material as authoritative project state.
